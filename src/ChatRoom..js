import { useHistory } from 'react-router-dom'
function ChatRoom(props) {
    const hs = useHistory();
    console.log(hs)
    return (
        <>
            ChatRoom
        </>
    );
}

export default ChatRoom;
